# Claude Code local tooling (worktrees, launch config, scratch)
.claude/

# Generated scratch report (the .html dashboard is published to the site)
link-report.md

# Backup archives — never commit (a 90MB .rar once bloated this repo)
*.rar
*.7z
